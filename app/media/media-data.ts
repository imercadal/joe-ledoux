export interface Media {
    id: string;
    title: string;
    mediaCompany: string;
    section: string;
    date: Date;
    tags: string[];
    urlToItem: string;
}

// #FALTA HACER ESTO
// UX: url a youtube que se pueda hacer click desde el nombre y bajada
//fecha, programa, episodio(title), tags, link

//new Date("2024-02-14")